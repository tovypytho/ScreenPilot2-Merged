name: Build ScreenPilot Android

on:
  workflow_dispatch:
  push:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: '21'

      - name: Set up Gradle
        uses: gradle/actions/setup-gradle@017a9effdb900e5b5b2fddfb590a105619dca3c3 # v4.4.2

      - name: Make Gradle wrapper executable
        run: chmod +x gradlew

      - name: Verify Gradle
        run: ./gradlew --version

      - name: Set up Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Prepare Flutter module
        run: |
          flutter config --no-analytics
          cd flutter_test_host
          flutter pub get
          flutter precache --android
          cd ..
          echo "flutter.sdk=$FLUTTER_ROOT" >> local.properties

      - name: Compile ScreenPilot
        run: ./gradlew :app:compileDebugKotlin --rerun-tasks

      - name: Run unit tests
        run: ./gradlew :app:testDebugUnitTest --rerun-tasks

      - name: Build debug APK
        run: ./gradlew :app:assembleDebug --rerun-tasks

      - name: Run lint
        run: ./gradlew :app:lintDebug --rerun-tasks

      - name: Upload ScreenPilot APK
        uses: actions/upload-artifact@v4
        with:
          name: ScreenPilot-debug-apk
          path: app/build/outputs/apk/debug/app-debug.apk
          if-no-files-found: error

      - name: Upload test and lint reports
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ScreenPilot-build-reports
          path: |
            app/build/test-results/
            app/build/reports/tests/
            app/build/reports/lint-results-debug.*
          if-no-files-found: ignore
